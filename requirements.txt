# Requirement versions should correspond to those available from latest Ubuntu LTS repositories

feedparser >= 5.2.1
# gconf >= 0.6.3    # Is this requirement needed?
gdata >= 2.0.18
# gio       # Has to be installed with apt
gobject
launchpadlib >= 1.10.6
lxml >= 4.2.1
pillow >= 5.1.0
# pygtk     # Has to be installed with apt
pyinotify >= 0.9.6
PySensors >= 0.0.4
pyudev >= 0.21.0
# pyusb
# pywapi >= 0.3.6   # Has to be installed with apt
pyxdg >= 0.26
setproctitle >= 1.1.10
six >= 1.11.0
# telepathy     # pip installation fails. Is this requirement needed?
xdg >= 4.0.1
Xlib >= 0.20
