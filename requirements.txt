feedparser >= 5.2.1
gconf
gdata >= 2.0.18
# gio       # Has to be installed with apt
gobject
launchpadlib >= 1.10.6
lxml >= 4.3.1
pillow >= 5.4.1
# pygtk     # Has to be installed with apt
pyinotify >= 0.9.6
PySensors >= 0.0.3
pyudev >= 0.21.0
# pywapi >= 0.3.6   # Has to be installed with apt
pyxdg >= 0.25.0
setproctitle >= 1.1.10
six >= 1.12.0
# telepathy     # pip installation fails. Is this requirement actually needed?
xdg
Xlib
